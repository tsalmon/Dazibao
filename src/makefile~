LDFLAGS=-Wall `pkg-config --cflags --libs gtk+-2.0`
CC=gcc
FILE=vue

main: $(FILE).o	
	$(CC) -g  -o $(FILE) $(FILE).o $(LDFLAGS)

$(FILE).o: $(FILE).c
	$(CC) -g $(FILE).c -c $(LDFLAGS)

clean:
	rm *.o